/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { LeadFormSubmissionService } from "../leadFormSubmission.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { LeadFormSubmissionCreateInput } from "./LeadFormSubmissionCreateInput";
import { LeadFormSubmission } from "./LeadFormSubmission";
import { LeadFormSubmissionFindManyArgs } from "./LeadFormSubmissionFindManyArgs";
import { LeadFormSubmissionWhereUniqueInput } from "./LeadFormSubmissionWhereUniqueInput";
import { LeadFormSubmissionUpdateInput } from "./LeadFormSubmissionUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class LeadFormSubmissionControllerBase {
  constructor(
    protected readonly service: LeadFormSubmissionService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: LeadFormSubmission })
  @nestAccessControl.UseRoles({
    resource: "LeadFormSubmission",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  @swagger.ApiBody({
    type: LeadFormSubmissionCreateInput,
  })
  async createLeadFormSubmission(
    @common.Body() data: LeadFormSubmissionCreateInput
  ): Promise<LeadFormSubmission> {
    return await this.service.createLeadFormSubmission({
      data: {
        ...data,

        pracitice: {
          connect: data.pracitice,
        },
      },
      select: {
        createdAt: true,
        email: true,
        emailConsented: true,
        firstName: true,
        id: true,
        interests: true,
        lastName: true,
        marketingConsented: true,
        mobileNumber: true,
        onlyInterestsConsented: true,

        pracitice: {
          select: {
            id: true,
          },
        },

        smsConsented: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [LeadFormSubmission] })
  @ApiNestedQuery(LeadFormSubmissionFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "LeadFormSubmission",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async leadFormSubmissions(
    @common.Req() request: Request
  ): Promise<LeadFormSubmission[]> {
    const args = plainToClass(LeadFormSubmissionFindManyArgs, request.query);
    return this.service.leadFormSubmissions({
      ...args,
      select: {
        createdAt: true,
        email: true,
        emailConsented: true,
        firstName: true,
        id: true,
        interests: true,
        lastName: true,
        marketingConsented: true,
        mobileNumber: true,
        onlyInterestsConsented: true,

        pracitice: {
          select: {
            id: true,
          },
        },

        smsConsented: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: LeadFormSubmission })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "LeadFormSubmission",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async leadFormSubmission(
    @common.Param() params: LeadFormSubmissionWhereUniqueInput
  ): Promise<LeadFormSubmission | null> {
    const result = await this.service.leadFormSubmission({
      where: params,
      select: {
        createdAt: true,
        email: true,
        emailConsented: true,
        firstName: true,
        id: true,
        interests: true,
        lastName: true,
        marketingConsented: true,
        mobileNumber: true,
        onlyInterestsConsented: true,

        pracitice: {
          select: {
            id: true,
          },
        },

        smsConsented: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: LeadFormSubmission })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "LeadFormSubmission",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  @swagger.ApiBody({
    type: LeadFormSubmissionUpdateInput,
  })
  async updateLeadFormSubmission(
    @common.Param() params: LeadFormSubmissionWhereUniqueInput,
    @common.Body() data: LeadFormSubmissionUpdateInput
  ): Promise<LeadFormSubmission | null> {
    try {
      return await this.service.updateLeadFormSubmission({
        where: params,
        data: {
          ...data,

          pracitice: {
            connect: data.pracitice,
          },
        },
        select: {
          createdAt: true,
          email: true,
          emailConsented: true,
          firstName: true,
          id: true,
          interests: true,
          lastName: true,
          marketingConsented: true,
          mobileNumber: true,
          onlyInterestsConsented: true,

          pracitice: {
            select: {
              id: true,
            },
          },

          smsConsented: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: LeadFormSubmission })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "LeadFormSubmission",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteLeadFormSubmission(
    @common.Param() params: LeadFormSubmissionWhereUniqueInput
  ): Promise<LeadFormSubmission | null> {
    try {
      return await this.service.deleteLeadFormSubmission({
        where: params,
        select: {
          createdAt: true,
          email: true,
          emailConsented: true,
          firstName: true,
          id: true,
          interests: true,
          lastName: true,
          marketingConsented: true,
          mobileNumber: true,
          onlyInterestsConsented: true,

          pracitice: {
            select: {
              id: true,
            },
          },

          smsConsented: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
